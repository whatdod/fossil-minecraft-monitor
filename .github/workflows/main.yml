name: Fossil Minecraft Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "NUOVO WORKFLOW TEST"
          echo "Se vedi questo messaggio, GitHub sta usando il file corretto."
